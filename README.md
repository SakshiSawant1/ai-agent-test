result = add_numbers(5, 3)
print(f"The result is: {result}")

This Python script defines a test function it doesn't do enough on its own to be useful. The script includes a simple function to add two numbers together, demonstrating Python's basic arithmetic functionality.